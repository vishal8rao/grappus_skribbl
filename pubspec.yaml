name: grappus_skribbl
description: A Grappus implementation of Skribbl
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  api:
    path: api
  bloc: ^8.1.2
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_localizations:
    sdk: flutter
  game_repository:
    path: packages/game_repository
  intl: ^0.18.0
  models:
    path: api/packages/models
  player_repository:
    path: api/packages/player_repository
  app_ui:
    path: packages/app_ui

dev_dependencies:
  bloc_test: ^9.1.2
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
